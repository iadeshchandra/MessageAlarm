<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="12dp"
    android:layout_marginVertical="4dp"
    app:cardCornerRadius="14dp"
    app:cardElevation="0dp"
    app:strokeWidth="1dp"
    app:strokeColor="?attr/colorOutlineVariant">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="14dp">

        <!-- Header row: app name + time -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginBottom="6dp">

            <TextView
                android:id="@+id/tvAppName"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:textSize="12sp"
                android:textAllCaps="true"
                android:letterSpacing="0.08"
                android:textColor="?attr/colorPrimary"
                android:textStyle="bold"
                tools:text="UPWORK" />

            <TextView
                android:id="@+id/tvTime"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="11sp"
                android:textColor="?attr/colorOnSurfaceVariant"
                tools:text="Mar 09, 14:32:10" />
        </LinearLayout>

        <!-- Chips row -->
        <com.google.android.material.chip.ChipGroup
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="6dp"
            app:singleLine="true">

            <com.google.android.material.chip.Chip
                android:id="@+id/chipBestMatch"
                android:layout_width="wrap_content"
                android:layout_height="26dp"
                android:text="⭐ Best Match"
                android:textSize="10sp"
                app:chipBackgroundColor="@color/upwork_green"
                android:textColor="@color/white"
                android:visibility="gone"
                tools:visibility="visible" />

            <com.google.android.material.chip.Chip
                android:id="@+id/chipUpwork"
                android:layout_width="wrap_content"
                android:layout_height="26dp"
                android:text="Upwork"
                android:textSize="10sp"
                app:chipBackgroundColor="?attr/colorSecondaryContainer"
                android:visibility="gone" />

            <com.google.android.material.chip.Chip
                android:id="@+id/chipFiverr"
                android:layout_width="wrap_content"
                android:layout_height="26dp"
                android:text="Fiverr"
                android:textSize="10sp"
                app:chipBackgroundColor="@color/fiverr_green"
                android:textColor="@color/white"
                android:visibility="gone" />
        </com.google.android.material.chip.ChipGroup>

        <!-- Title -->
        <TextView
            android:id="@+id/tvTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="14sp"
            android:textStyle="bold"
            android:maxLines="1"
            android:ellipsize="end"
            tools:text="New job: Android Developer needed" />

        <!-- Content -->
        <TextView
            android:id="@+id/tvContent"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="12sp"
            android:textColor="?attr/colorOnSurfaceVariant"
            android:maxLines="2"
            android:ellipsize="end"
            android:layout_marginTop="2dp"
            tools:text="Senior Android Developer for a 3-month contract project. Kotlin required." />
    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
